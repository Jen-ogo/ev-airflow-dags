This repo contains ONLY project DAGs and tools:
- `dags/`  — Airflow DAGs package (mounted into `/opt/airflow/dags/ev_repository/dags`)
- `tools/` — utilities (docs generator, etc.)

Secrets and docker-compose are intentionally NOT included.
